apiVersion: v1
kind: Service
metadata:
  name: myproject-customer-service
spec:
  selector:
    app: myproject-customer-service
  type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 80
      targetPort: 5000